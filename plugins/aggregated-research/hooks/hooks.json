{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "$comment": "No dedicated hooks-only schema exists; this file is a valid subset of the Claude Code settings schema, whose hooks property defines these values.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/bin/mise-env\" install --yes && echo \"aggregated-research: CLI ready — run: aggregated-research --help\""
          }
        ]
      }
    ]
  }
}
