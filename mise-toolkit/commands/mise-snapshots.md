---
description: Update cargo insta snapshots for jdx/mise
---

Run `mise run snapshots` to review and accept `cargo insta` snapshot changes. **Inspect the diff before accepting** — snapshot drift can hide real CLI output regressions.
