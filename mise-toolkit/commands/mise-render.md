---
description: Regenerate jdx/mise docs, completions, and JSON schema
---

Run `mise run render`. This regenerates CLI usage docs, shell completions, mangen pages, and the JSON schema. Run this after any change to `src/cli/**` or `settings.toml` so the committed artifacts stay in sync with the code.
